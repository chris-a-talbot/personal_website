type ScrollProgressOptions = {
    threshold?: number;
    smooth?: boolean;
    smoothingFactor?: number;
};
export declare const useScrollProgress: ({ threshold, smooth, smoothingFactor }?: ScrollProgressOptions) => number;
export {};
